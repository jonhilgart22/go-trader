We store the config files here to match the read/write locations on lambda. Otherwise, it gets confusing..


Note
- All new filename for coins need to start with `coin_` . For example, `ada_` or `sol_`
